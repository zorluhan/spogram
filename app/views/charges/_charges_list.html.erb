
<% if (instauser_logged_in? && !branduser_logged_in?) %>

    <% if @charges.empty? %>
                       <div class="row row0">
    <div class="col-sm-12 col-xs-12 text-center proposalboxempty">      
       You have no proposals yet.
    </div>
  </div>
    
    <% else %>
          <br>
          <%= render :partial=> "all_charges_list" %>
    <%end%>

</div>
</div>
 
 
 
<% elsif branduser_logged_in? && !instauser_logged_in? %>

          <% if @charges.empty? %>
                  <div class="row row0">
    <div class="col-sm-12 col-xs-12 text-center proposalboxempty">      
       You have no proposals yet.
    </div>
  </div>
          <% else %>
          <br>
         <%= render :partial=> "all_charges_list" %>
         <%end%>
 </div>
</div>
 
 
<% else %>
  <% redirect_to root_path %>
<% end %>
 
 